{
  "name": "red-contrib-better-queue",
  "version": "1.0.0",
  "description": "Better Queue library implementation in node-red",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "better-queue": "^3.8.12"
  },
  "node-red": {
    "nodes": {
      "queue": "better-queue.js",
      "retry-config": "retry-config/retry-config.js"
    }
  }
}